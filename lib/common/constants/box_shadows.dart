import 'package:flutter/material.dart';

class AppBoxShadows {
  BoxShadow get small => BoxShadow(
        color: Colors.black.withOpacity(0.2),
        blurRadius: 5.0,
        spreadRadius: 0.5,
      );
}
